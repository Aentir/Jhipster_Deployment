apply plugin: "war"

bootWar {
    mainClass = "com.trasteoapp.com.TrasteoApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
